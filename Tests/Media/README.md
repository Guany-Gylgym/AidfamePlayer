# Media Fixtures

Run scripts/generate-fixtures.ps1 to generate local synthetic 12-second H.264 1080p and HEVC Main10 4K clips with AAC sine audio. These are deterministic test patterns, not real camera footage. Media binaries are excluded from Git. Real camera clips and sustained-performance samples remain release coverage requirements.
